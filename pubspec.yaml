name: bubble_splash
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flame: ^1.37.0
  flame_audio: ^2.12.1
  shared_preferences: ^2.5.5
  flutter_riverpod: ^3.3.2
  google_fonts: ^8.1.0
  supabase_flutter: ^2.8.0
  google_mobile_ads: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.4

# App icon generation (run: dart run flutter_launcher_icons).
# Source art in assets/icon/ (copied from the design handoff — do NOT re-render).
# web: omitted deliberately — web is not a target platform here (no web/ dir).
# iOS icons are generated for the parked scaffold; iOS build verification needs macOS.
# adaptive_icon_foreground_inset: 10 — the handoff foreground is near full-bleed (NOT in a 66%
#   safe zone as its README claimed). Tool default 16% left a dark margin ring; 0% clipped the
#   big orange bubble against the circle mask (orange rim). 10% keeps bubbles inside the circle
#   with a thin star-field margin (reads as space). Tune here if a launcher mask crops differently.
# adaptive_icon_background uses the designer's gradient PNG (matches the master), not flat #120830.
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  background_color_ios: "#120830"
  adaptive_icon_background: "assets/icon/adaptive_background.png"
  adaptive_icon_foreground: "assets/icon/adaptive_foreground.png"
  adaptive_icon_foreground_inset: 10

# OS cold-start frame (static, no animation) — covers the first launch moment
# before the animated Flutter SplashScreen mounts. Regenerate with:
#   dart run flutter_native_splash:create
#
# Deliberately NO logo: the cold-start frame is just the flat cosmos color
# (#120830), matching the animated SplashScreen's background, so the two read as
# a SINGLE splash (the animated bubbles/wordmark fade in on the same colour)
# instead of "icon splash → different animated splash". On Android 12+ the OS
# splash API forces a centered icon, so we feed it a fully transparent image
# (assets/icon/splash_transparent.png) to suppress the logo — leaving only the
# background colour. Legacy (<12) is color-only already.
flutter_native_splash:
  color: "#120830"
  android_12:
    color: "#120830"
    icon_background_color: "#120830"
    image: assets/icon/splash_transparent.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/audio/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

dependency_overrides:
  audioplayers_android:
    git:
      url: https://github.com/bluefireteam/audioplayers.git
      path: packages/audioplayers_android
      ref: main
